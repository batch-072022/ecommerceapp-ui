import { SearchBySellerPipe } from './search-by-seller.pipe';

describe('SearchBySellerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBySellerPipe();
    expect(pipe).toBeTruthy();
  });
});
